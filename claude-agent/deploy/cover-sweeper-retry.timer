[Unit]
# 撞 429 的条目在 waiting/ 里记着 notBefore，这里半小时看一次有没有到点的搬回 pending/。
# waiting/ 为空时 sweeper 几十毫秒退出，不算浪费。
Description=Wake parked cover jobs every 30 minutes

[Timer]
OnCalendar=*:0/30
RandomizedDelaySec=60
Unit=cover-sweeper.service

[Install]
WantedBy=timers.target
